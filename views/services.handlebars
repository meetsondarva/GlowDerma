<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Services</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #e7f5e1; /* Light greenish background */
            margin: 0;
            padding: 0;
            color: #4e4e4e;
        }

        h1 {
            text-align: center;
            color: #00bfae; /* Teal color for the heading */
            margin-top: 50px;
            font-size: 40px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        p {
            text-align: center;
            font-size: 20px;
            margin: 20px auto;
            max-width: 900px;
            line-height: 1.8;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        p:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.15);
        }

        .category-container {
            padding: 40px;
        }

        /* Unique button for contact action */
        .contact-btn {
            display: block;
            width: 200px;
            margin: 30px auto;
            padding: 15px;
            text-align: center;
            background-color: #00bfae;
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .contact-btn:hover {
            background-color: #008e7d;
            transform: translateY(-5px);
        }

        /* Responsive design for smaller screens */
        @media screen and (max-width: 600px) {
            h1 {
                font-size: 32px;
            }

            p {
                font-size: 18px;
                padding: 15px;
            }

            .contact-btn {
                width: 160px;
                font-size: 16px;
            }
        }
    </style>
</head>
<body>

    <div class="category-container">
        <h1>{{category}} Services</h1>
        <p>Explore the various services we offer in the {{category}} category. Contact us for more information!</p>
        <button class="contact-btn">Contact Us</button>
    </div>

</body>
</html>
